<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Category List</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 flex items-center justify-center min-h-screen">

    <div class="w-full max-w-4xl p-8 bg-white shadow-md rounded-lg">
        <h1 class="text-3xl font-semibold text-gray-900 mb-6">Category List</h1>
        <a href="../dashboard">
            <button type="submit" class="bg-green-500 text-white  px-4 py-2  rounded-md hover:bg-red-600">Dashboard</button>

        </a>
        <a href="/category/add" class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 mb-6 inline-block">Add New Category</a>
        <ul class="space-y-4">
            <% categories.forEach(category => { %>
                <li class="bg-white shadow-md rounded-md p-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <h3 class="text-xl font-semibold"><%= category.name %></h3>
                            <p><strong>Subcategory:</strong> <%= category.subcategory %></p>
                            <p><strong>Seller Name:</strong> <%= category.sellerName %></p>
                        </div>
                        <div class="space-x-2">
                            <a href="/category/edit/<%= category._id %>" class="bg-yellow-500 text-white px-3 py-1 rounded-md hover:bg-yellow-600">Edit</a>
                            <form action="/category/delete/<%= category._id %>" method="POST" style="display:inline;">
                                <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded-md hover:bg-red-600">Delete</button>
                            </form>
                        </div>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>
    
</body>
</html>
