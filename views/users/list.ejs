<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body className="bg-gray-100 text-gray-800">

    <div className="container mx-auto p-6">
        <h1 class="text-3xl font-bold mb-6">User List</h1>
        <a href="../dashboard">
            <button type="submit" class="bg-green-500 text-white  px-4 py-2  rounded-md hover:bg-red-600">Dashboard</button>

        </a>
        <a href="/users/add" class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 mb-4 inline-block">Add New User</a>
        
        <ul class="list-disc pl-5 space-y-4">
            <% users.forEach(user => { %>
                <li class="bg-white shadow-md rounded-md p-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <h2 class="text-xl font-semibold"><%= user.firstName %> <%= user.lastName %></h2>
                            <p class="text-gray-600"><%= user.role %> - <%= user.email %></p>
                        </div>
                        <div class="space-x-2">
                            <a href="/users/edit/<%= user._id %>" class="bg-yellow-500 text-white px-3 py-1 rounded-md hover:bg-yellow-600">Edit</a>
                            <form action="/users/delete/<%= user._id %>" method="POST" style="display:inline;">
                                <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded-md hover:bg-red-600">Delete</button>
                            </form>
                        </div>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>
    <footer class="bg-gray-900 text-white p-6 mt-12">
        <div class="container mx-auto text-center">
            <p class="text-sm">&copy; 2024 Smart Management Billing System. All Rights Reserved.</p>
            <p class="text-xs mt-1">Designed with ❤️</p>
        </div>
    </footer>

</body>
</html>
